export const register = (e:any, email?:string, password?:string, repeatPassword?: string) => {
    e.preventDefault();
    console.log(email, password)
    // return { type: 'ERROR', message: "Coś poszło nie tak"}
}